name: sunmi_pothys_parking_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  screenshot: ^3.0.0
  http: ^1.2.0
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  path: ^1.9.0
  shared_preferences: ^2.2.3
  qr_flutter: ^4.1.0
  sunmi_printer_plus: ^2.1.3
  image: ^4.1.7
  barcode_image: ^2.0.3
  intl: ^0.19.0
  ai_barcode_scanner: ^5.1.1+2
  device_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
